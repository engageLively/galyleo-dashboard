# Dependencies
node_modules/

# Build output
app/dist/
app/.vite/

# Test coverage
coverage/

# Env / local config
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
*.suo
*.user

# Claude Code local settings (project settings only — CLAUDE.md is committed)
.claude/settings.json
.claude/settings.local.json

# Deployment scripts (these live in the deployment repo)
build-for-jh2.bat
redirect-stub.html

# Jupyter notebooks
*.ipynb
.ipynb_checkpoints/

# TypeDoc output
docs/api/

# Root-level legacy / local artifacts (not part of the React app)
galyleo-client/
galyleo-data/
jupyterlab-universal-extension/
jupyterlab_universal_extension/
.devcontainer/
package-lock.json
!app/package-lock.json

# Local standalone config (each dev sets their own)
app/public/galyleo.config.json

# Runtime dashboard files (scratch / test data)
*.gd.json
*.txt
